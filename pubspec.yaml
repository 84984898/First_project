name: flutter_application_1
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
    
  cupertino_icons: ^1.0.0
  like_button: ^2.0.2
  
  # Добавили зависимость библиотеки Dio
  dio: ^4.0.0
 
  hive: ^2.0.4 
  hive_flutter: ^1.1.0

  
dev_dependencies:

  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/icons/
